<h2>Edit Post</h2>

<%= render "form.html", Map.put(assigns, :action, post_path(@conn, :update, @post)) %>

<span><%= link gettext("Back"), to: post_path(@conn, :index), class: "button is-danger" %></span>
